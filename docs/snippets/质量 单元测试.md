#   单元测试

script

```bash
"test": "istanbul cover _mocha test/*"
```

dependencies

```bash
npm i chai istanbul mocha
```

test/file.js

```javascript
const fse = require('fs-extra');
const path = require('path');
const fs = require('fs');

const { expect } = require('chai');
const fileUtil = require('./file');

describe('test ./file.js xxx', () => {
    beforeEach(() => {
        // ...
    });

    afterEach(() => {
        // ...
    });

    it('test item 0', () => {
        expect(xxx).to.be.deep.equal(xxx);
    });

    it('test item 1', () => {
        expect(xxx).to.be.deep.equal(xxx);
    });
});
```